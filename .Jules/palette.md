## 2025-05-14 - Redundant A11y Utilities
**Learning:** Tailwind CSS already provides the `sr-only` utility for visually hidden but accessible content. Manually defining it in `globals.css` is redundant and can lead to style conflicts.
**Action:** Always leverage Tailwind's native utility classes for accessibility before adding custom CSS.
